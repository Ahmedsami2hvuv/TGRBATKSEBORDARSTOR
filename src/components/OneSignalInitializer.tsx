"use client";

// تصدير مسمى (Named Export) ليتوافق مع client-runtime.tsx
export function OneSignalInitializer({ externalId }: { externalId?: string }) {
  // الكود معطل حالياً لضمان استقرار الموقع 100%
  return null;
}
