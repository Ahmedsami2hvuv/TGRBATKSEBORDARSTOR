# تقرير الإنجاز: نظام الخلفيات المخصصة للنظام (محدث - حل تداخل القائمة الجانبية)

تم تطوير وتحديث نظام الخلفيات المخصصة بنجاح في جميع واجهات النظام. يتيح هذا النظام لإدارة الموقع رفع صور الخلفيات المرغوبة، ويسمح لجميع المستخدمين بتفعيل الخلفيات المفضلة لديهم محلياً على أجهزتهم بطريقة عصرية مبهرة تعتمد على التأثير الزجاجي (Glassmorphism).

---

## التعديلات والإجراءات الأخيرة (تحديث القائمة الجانبية واستبعاد العملاء)

### 1. حل مشكلة تداخل القائمة الجانبية (Aside Sidebar) بالإدارة
- **المشكلة**: كانت القائمة الجانبية شفافة تماماً للوراء، مما كان يؤدي لتداخل عناصر وجدول الصفحة الرئيسية مع أزرار وأيقونات القائمة الجانبية، مما يعيق القراءة ويحدث تشتتاً بصرياً.
- **الحل الجذري**: 
  1. تم ربط القائمة الجانبية (`aside`) بمستمع الخلفية المخصصة وجعلها **تحمل وتطبق صورة الخلفية المفعلة نفسها كخلفية خاصة بها (`background-image`)!**
  2. تم تغليف القائمة الجانبية بطبقة زجاجية ناعمة وشبه شفافة (بيضاء خفيفة في الوضع الفاتح وداكنة خفيفة في الوضع الداكن) فوق صورتها الخلفية لضمان قراءة النصوص ووضوح الأيقونات بشكل ممتاز.
  3. بفضل هذا الحل، تحجب القائمة الجانبية تماماً أي محتوى يمر خلفها، فلا يحدث أي تداخل بصري وتظهر القائمة وهي تكتسي بصورة الخلفية المفعّلة كخلفية متناسقة لوحدها!

### 2. استبعاد بوابة العملاء وأصحاب المحلات (Client Portal)
- تم برمجة استثناء شامل لبوابة أصحاب المحلات والعملاء الذين يرفعون طلباتهم (أي مسار يبدأ بـ `/client`).
- تظل الخلفية لديهم بيضاء تماماً وثابتة ونظيفة، ولا تتأثر بنظام الخلفيات المخصصة ولا بالخلفية الافتراضية للنظام التي تفعلها الإدارة.

---

## الإجراءات السابقة والتغييرات التي تم تنفيذها

### 1. قاعدة البيانات وقنوات التخزين
- **مخطط قاعدة البيانات (schema.prisma)**: تم إضافة جدول `SystemBackground` لتخزين معرفات وصور الخلفيات المرفوعة مع حقل `active` لتحديد الخلفية الافتراضية للنظام ككل.
- **توليد Prisma Client**: تم توليد العميل محلياً بنجاح لتفادي أي أخطاء، وسيتم تحديث الجداول على السيرفر تلقائياً بمجرد رفع الكود (بوش).

### 2. التنسيقات العالمية والمكونات المشتركة
- **globals.css**: 
  1. جعل خلفية الـ `body` و الـ `html` شفافة تماماً عند تفعيل الخلفية المخصصة (`body.has-custom-bg`).
  2. تحويل جميع الصناديق والكروت البيضاء والرمادية المطبقة بـ `bg-white` أو `bg-slate-50` إلى كروت شبه شفافة (Glassmorphic) تلقائياً لتتكامل بشكل بديع وراقي مع الصورة الخلفية وتضمن قراءتها.
  3. إدخال تأثيرات الشفافية والزجاج (Backdrop-filter blur) على الخلفيات.
- **static-background.tsx**: يقرأ الخلفية المفضلة للمستخدم من التخزين المحلي (LocalStorage) وإذا لم تكن موجودة يطبق الخلفية الافتراضية للنظام المفعّلة من السيرفر.

### 3. إعدادات الإدارة (رفع وإدارة الصور)
- **background-actions.ts**: ملف إجراءات خادم (Server Actions) لرفع الصور وضغطها تلقائياً بالاعتماد على R2، وتفعيل الخلفية على مستوى النظام ككل في قاعدة البيانات (`setSystemDefaultBackgroundAction`).
- **admin-backgrounds-settings.tsx**: واجهة متكاملة تظهر في تبويب "الخلفيات المخصصة" بالإعدادات، تتيح للمسؤول:
  1. رفع خلفية جديدة مع تسميتها.
  2. حذف الخلفيات الموجودة.
  3. تفعيل الخلفية كخلفية افتراضية للنظام ككل في قاعدة البيانات.

### 4. واجهات باقي أطراف النظام
- **المندوب (mandoub/settings)**: إضافة قسم "خلفية التطبيق" في صفحة إعدادات المندوب يتيح له تفعيل أي خلفية مرفوعة أو إلغائها.
- **المجهز (preparer/settings)**: إضافة قسم "خلفية التطبيق" في إعدادات المجهز بنفس الطريقة.
- **الموظف (staff/portal)**: إضافة واجهة اختيار الخلفية المخصصة في البوابة الرئيسية للموظف مباشرة.
- **المورد (supplier/page.tsx)**: إضافة واجهة اختيار الخلفية في بوابة المورد الرئيسية مباشرة.
