const { PrismaClient } = require("@prisma/client"); const prisma = new PrismaClient(); async function main() { const records = await prisma.schemaPlaceholder.findMany({ where: { note: { startsWith: "scheduled_alert:" } } }); console.log("RECORDS_START"); console.log(JSON.stringify(records)); console.log("RECORDS_END"); } main().catch(console.error).finally(() => prisma.$disconnect());
